---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "clearml_resource_policy Resource - terraform-provider-clearml"
subcategory: ""
description: |-
  A ClearML Enterprise resource policy for one user group. The vendor-managed resource policy manager must be enabled.
---

# clearml_resource_policy (Resource)

A ClearML Enterprise resource policy for one user group. The vendor-managed resource policy manager must be enabled.

## Example Usage

```terraform
data "clearml_user_group" "ml_engineers" {
  name = "ML Engineers"
}

# The ClearML vendor administrator must enable the resource policy manager.
resource "clearml_resource_policy" "interactive_gpu" {
  name          = "interactive-gpu"
  description   = "Reserved interactive GPU capacity"
  reservation   = 1
  limit         = 4
  user_group_id = data.clearml_user_group.ml_engineers.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `limit` (Number)
- `name` (String)
- `reservation` (Number)
- `user_group_id` (String)

### Optional

- `description` (String) Policy description. Omit it to preserve the remote value.

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import clearml_resource_policy.interactive_gpu resource-policy-id
```
